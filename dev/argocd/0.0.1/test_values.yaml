# Default values for emby.

ingress:
  main:
    enabled: true
    certType: selfsigned
    type: "HTTP"
    serviceName: "argocd-server"
    servicePort: 80
    entrypoint: "websecure"
    hosts:
      - host: "test.test.com"
        path: "/"

argo-cd:
  fullnameOverride: argocd
