apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "truecommand.fullname" . }}
  labels:
    {{- include "truecommand.labels" . | nindent 4 }}
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: {{ .Values.strategyType }}
  selector:
    matchLabels:
      {{- include "truecommand.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "truecommand.selectorLabels" . | nindent 8 }}
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
    spec:
    {{- if .Values.hostNetwork }}
      hostNetwork: {{ .Values.hostNetwork }}
      dnsPolicy: ClusterFirstWithHostNet
    {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
          - name: truecommand
            protocol: TCP
            containerPort: 80
          {{- if .Values.hostNetwork }}
            hostPort: {{ .Values.truecommandService.port }}
          {{- end }}
          env:
          - name: TZ
            value: "{{ .Values.timezone }}"
          # truecommand env vars
          - name: TRUECOMMAND_INTERNAL_ADDRESS
            value: http://{{ template "truecommand.fullname" . }}:80
          - name: TRUECOMMAND_IMAGE
            value: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          - name: KUBE_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace

# Extra ENV Values supplied by user
{{- range $key, $value := .Values.extraEnv }}
          - name: {{ $key }}
            value: {{ $value }}
{{- end }}

#          readinessProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            failureThreshold: 5
#            periodSeconds: 15
#          livenessProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            failureThreshold: 5
#            periodSeconds: 15
#          startupProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            initialDelaySeconds: 5
#            failureThreshold: 40
#            periodSeconds: 15
          volumeMounts:
#          - name: data
#            mountPath: /data
          - name: config
            mountPath: /data
#          - name: downloads
#            mountPath: /downloads
          - name: shared
            mountPath: /shared
          - name: shared-logs
            mountPath: "/config/Library/Application Support/sonarr Media Server/Logs"
      volumes:
      - name: config
        {{- if .Values.emptyDirVolumes }}
        emptyDir: {}
        {{- else }}
        hostPath:
          path: {{ template "configuredHostPathConfig" . }}
        {{- end }}
      - name: shared
        emptyDir: {}
      - name: shared-logs
        emptyDir: {}
