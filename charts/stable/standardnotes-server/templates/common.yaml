{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Render secrets for standardnotes */}}
{{- $secrets := include "standardnotes.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "standardnotes-secrets" $secrets -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
