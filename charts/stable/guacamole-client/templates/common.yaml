{{/* Make sure all variables are set properly */}}
{{ include "tc.v1.common.loader.init" . }}

{{/* Render configmap for guacamole-client */}}
{{- $configmap := include "guacamole-client.configmap" . | fromYaml -}}
{{- if $configmap -}}
{{- $_ := set .Values.configmap "env" $configmap -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
