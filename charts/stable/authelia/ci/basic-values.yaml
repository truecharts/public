command:
  - cat
  - /configuration.yaml
args: []

access_control:
  rules:
  - domain: domain.com
    policy: bypass
    resources:
      - "^/api.*"
  - domain: "*.domain.com"
    policy: bypass
    resources:
      - ^/api.*
