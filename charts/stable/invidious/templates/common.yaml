{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Render configmap for invidiuous */}}
{{- $configmap := include "invidious.config" . | fromYaml -}}
{{- if $configmap -}}
{{- $_ := set .Values.configmap "invidious-config" $configmap -}}
{{- end -}}

{{- $_ := mustMergeOverwrite .Values.workload.main.podSpec (include "invidious.dbseed" . | fromYaml) -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
