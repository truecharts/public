image:
  repository: tccr.io/truecharts/TEMPLATEREPLACEME
  tag: TEMPLATEREPLACEME@sha256:TEMPLATEREPLACEME
  pullPolicy: IfNotPresent

# securityContext:
#   readOnlyRootFilesystem: false
#   runAsNonRoot: false

# podSecurityContext:
#   runAsUser: 0
#   runAsGroup: 0

env:
  {}

service:
  main:
    ports:
      main:
        port: 10xxx
        targetPort: 0

persistence:
  config:
    enabled: true
    mountPath: "/config"
