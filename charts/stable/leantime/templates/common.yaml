{{- include "tc.v1.common.loader.init" . }}

{{/* Render secrets for leantime */}}
{{- $secrets := include "leantime.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "secrets" $secrets -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
