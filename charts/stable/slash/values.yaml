image:
  repository: yourselfhosted/slash
  pullPolicy: IfNotPresent
  tag: 1.0.0@sha256:8730811e91861849fdabfc63e44872bd0815d7c89725f6ceaab0fd4974f49985
service:
  main:
    ports:
      main:
        protocol: http
        port: 5231
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            SLASH_PORT: "{{ .Values.service.main.ports.main.port }}"
persistence:
  config:
    enabled: true
    mountPath: "/var/opt/slash"
portal:
  open:
    enabled: true
