image:
  repository: tagspaces/tagspaces-lite-web
  pullPolicy: IfNotPresent
  tag: 6.0.1@sha256:08cf8353aa1a95a55dd74404728ce0e7e3b3d37ae210c4d626502e16f87dfd9f
securityContext:
  container:
    readOnlyRootFilesystem: false
service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 80
        port: 5000
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            KEY:
              secretKeyRef:
                name: tagspaces-secret
                key: KEY
persistence:
  nginx:
    enabled: true
    type: emptyDir
    mountPath: "/var/cache/nginx"
portal:
  open:
    enabled: true
