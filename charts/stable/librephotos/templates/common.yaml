{{- include "tc.v1.common.loader.init" . }}

{{- $secrets := include "librephotos.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "librephotos-secrets" $secrets -}}
{{- end -}}

{{ include "tc.v1.common.loader.apply" . }}
