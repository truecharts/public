{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Disable [ranksystem-cron] if requested */}}
{{- if not .Values.ranksystem.cron_enabled -}}
  {{- $_ := set .Values.workload.ranksystem-cron "enabled" false -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
