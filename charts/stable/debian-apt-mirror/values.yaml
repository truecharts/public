image:
  pullPolicy: IfNotPresent
  repository: ich777/debian-mirror
  tag: latest@sha256:f403f400567e6db13cad70f2927f8d1429bfc7462a6b54ad2785270f6de9b4a4
persistence:
  config:
    enabled: true
    mountPath: /debian-mirror/config
  data:
    enabled: true
    mountPath: /debian-mirror/data
  base:
    enabled: true
    mountPath: /debian-mirror
portal:
  open:
    enabled: true
securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsGroup: 0
    runAsUser: 0
service:
  main:
    ports:
      main:
        port: 980
        protocol: tcp
        targetPort: 980
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            DATA_DIR: "/debian-mirror"
            CONFIG_DIR: "/debian-mirror/config"
            MIRROR_DIR: "/debian-mirror/data"
            APACHE2_PORT: "980"
            CRON_SCHEDULE: "0 1 * * *"
            FORCE_UPDATE: "false"
            UID: "{{ .Values.securityContext.container.PUID }}"
            GID: "{{ .Values.securityContext.pod.fsGroup }}"
            UMASK: "{{ .Values.securityContext.container.UMASK }}"
            DATA_PERM: "770"
