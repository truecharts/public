image:
  repository: docker.io/fireflyiii/core
  pullPolicy: IfNotPresent
  tag: version-6.2.10@sha256:68de37b265ff190c6df384b9ffe297e3e9f52e31a6229112ff2e8767fef540ee
securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsUser: 33
    runAsGroup: 33
workload:
  main:
    podSpec:
      containers:
        main:
          probes:
            liveness:
              path: "/healthcheck"
            readiness:
              path: "/healthcheck"
            startup:
              path: "/healthcheck"
          env:
            DB_USERNAME: "{{ .Values.cnpg.main.user }}"
            DB_DATABASE: "{{ .Values.cnpg.main.database }}"
            DB_CONNECTION: pgsql
            DB_PORT: 5432
            REDIS_DB: "0"
            REDIS_CACHE_DB: "1"
            CACHE_DRIVER: redis
            SESSION_DRIVER: redis
            REDIS_SCHEME: tcp
            REDIS_PORT: 6379
            APP_URL: ""
            TRUSTED_PROXIES: "172.16.0.0/16"
            DB_HOST:
              secretKeyRef:
                name: cnpg-main-urls
                key: host
            DB_PASSWORD:
              secretKeyRef:
                name: cnpg-main-user
                key: password
            REDIS_HOST:
              secretKeyRef:
                expandObjectName: false
                name: '{{ printf "%s-%s" .Release.Name "rediscreds" }}'
                key: plainhost
            REDIS_PASSWORD: "{{ .Values.redis.password }}"
            STATIC_CRON_TOKEN:
              secretKeyRef:
                name: firefly-secrets
                key: STATIC_CRON_TOKEN
            APP_KEY:
              secretKeyRef:
                name: firefly-secrets
                key: APP_KEY
service:
  main:
    ports:
      main:
        port: 10082
        targetPort: 8080
persistence:
  data:
    enabled: true
    mountPath: "/var/www/html/storage/upload"
  varrun:
    enabled: false
cronjob:
  schedule: "0 3 * * *"
  annotations: {}
  failedJobsHistoryLimit: 5
  successfulJobsHistoryLimit: 2
# Enabled redis
redis:
  enabled: true
  includeCommon: true
# Enabled postgres
cnpg:
  main:
    enabled: true
    user: firefly
    database: firefly
portal:
  open:
    enabled: true
