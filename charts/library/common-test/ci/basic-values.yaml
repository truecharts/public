image:
  repository: b4bz/homer
  tag: latest
  pullPolicy: IfNotPresent

services:
  main:
    port:
      port: 8080

ingress:
  main:
    enabled: true

probes:
  liveness:
    enabled: true
  readiness:
    enabled: true
  startup:
    enabled: true
