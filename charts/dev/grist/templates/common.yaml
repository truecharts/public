{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Render secrets for grist */}}
{{- $secrets := include "grist.secret" . | fromYaml -}}
{{- if $secrets -}}
{{- $_ := set .Values.secret "grist-secret" $secrets -}}
{{- end -}}

{{/* Render configmap for grist */}}
{{- $configmap := include "grist.config" . | fromYaml -}}
{{- if $configmap -}}
{{- $_ := set .Values.configmap "grist-config" $configmap -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
