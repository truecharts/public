image:
  pullPolicy: IfNotPresent
  repository: openproject/community
  tag: 13.1.2@sha256:8636cfc514e012f6f1b34d2f5400d89f648b8b9ca329579114bdf3c7675b076a
persistence:
  assets:
    enabled: true
    mountPath: /var/openproject/assets
portal:
  open:
    enabled: true
cnpg:
  main:
    enabled: true
    database: openproject
    user: openproject
securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsGroup: 0
    runAsUser: 0
service:
  main:
    ports:
      main:
        port: 10290
        targetPort: 80
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            DATABASE_URL:
              secretKeyRef:
                key: url-noql
                name: dbcreds
            SECRET_KEY_BASE:
              secretKeyRef:
                key: SECRET_KEY_BASE
                name: openproject-secrets
