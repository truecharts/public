# Include{groups}
portals:
  open:
    protocols:
      - "$kubernetes-resource_configmap_portal_protocol"
    host:
      - "$kubernetes-resource_configmap_portal_host"
    ports:
      - "$kubernetes-resource_configmap_portal_port"
questions:
  - variable: portal
    group: "Container Image"
    label: "Configure Portal Button"
    schema:
      type: dict
      hidden: true
      attrs:
        - variable: enabled
          label: "Enable"
          description: "enable the portal button"
          schema:
            hidden: true
            editable: false
            type: boolean
            default: true
# Include{global}

  - variable: controller
    group: "Controller"
    label: ""
    schema:
      additional_attrs: true
      type: dict
      attrs:
        - variable: advanced
          label: "Show Advanced Controller Settings"
          schema:
            type: boolean
            default: false
            show_subquestions_if: true
            subquestions:
              - variable: type
                description: "Please specify type of workload to deploy"
                label: "(Advanced) Controller Type"
                schema:
                  type: string
                  default: "deployment"
                  required: true
                  enum:
                    - value: "deployment"
                      description: "Deployment"
                    - value: "statefulset"
                      description: "Statefulset"
                    - value: "daemonset"
                      description: "Daemonset"
              - variable: replicas
                description: "Number of desired pod replicas"
                label: "Desired Replicas"
                schema:
                  type: int
                  default: 1
                  required: true
              - variable: strategy
                description: "Please specify type of workload to deploy"
                label: "(Advanced) Update Strategy"
                schema:
                  type: string
                  default: "Recreate"
                  required: true
                  enum:
                    - value: "Recreate"
                      description: "Recreate: Kill existing pods before creating new ones"
                    - value: "RollingUpdate"
                      description: "RollingUpdate: Create new pods and then kill old ones"
                    - value: "OnDelete"
                      description: "(Legacy) OnDelete: ignore .spec.template changes"
# Include{controllerExpert}

  - variable: env
    group: "Container Configuration"
    label: "Image Environment"
    schema:
      additional_attrs: true
      type: dict
      attrs:

        - variable: DB_DRIVER
          label: "DB_DRIVER"
          description: "Container Variable DBDRIVER"
          schema:
            type: string
            default: "mysql"
        - variable: APP_KEY
          label: "APP_KEY"
          description: "Container Variable APPKEY. Some random string"
          schema:
            type: string
            default: "base64:2iJWPWu9PWzkI4B2CcdVYQo0CPvtXIhS1vVi/GCGn5A="
        - variable: DB_HOST
          label: "DB_HOST"
          description: "Container Variable DBHOST"
          schema:
            type: string
            default: "192.168.1.100"
        - variable: DB_DATABASE
          label: "DB_DATABASE"
          description: "Container Variable DBDATABASE"
          schema:
            type: string
            default: "cachet"
        - variable: DB_USERNAME
          label: "DB_USERNAME"
          description: "Container Variable DBUSERNAME"
          schema:
            type: string
            default: "cachet"
        - variable: DB_PASSWORD
          label: "DB_PASSWORD"
          description: "Container Variable DBPASSWORD"
          schema:
            type: string
            default: "password"
        - variable: DB_PORT
          label: "DB_PORT"
          description: "Container Variable DBPORT"
          schema:
            type: string
            default: "3306"
        - variable: MAIL_DRIVER
          label: "MAIL_DRIVER"
          description: "Container Variable MAILDRIVER"
          schema:
            type: string
            default: "smtp"
        - variable: MAIL_HOST
          label: "MAIL_HOST"
          description: "Container Variable MAILHOST"
          schema:
            type: string
            default: "smtp.mailgun.org"
        - variable: MAIL_PORT
          label: "MAIL_PORT"
          description: "Container Variable MAILPORT"
          schema:
            type: string
            default: "587"
        - variable: MAIL_USERNAME
          label: "MAIL_USERNAME"
          description: "Container Variable MAILUSERNAME"
          schema:
            type: string
            default: "postmaster@domain.com"
        - variable: MAIL_PASSWORD
          label: "MAIL_PASSWORD"
          description: "Container Variable MAILPASSWORD"
          schema:
            type: string
            default: "password"
        - variable: MAIL_ADDRESS
          label: "MAIL_ADDRESS"
          description: "Container Variable MAILADDRESS"
          schema:
            type: string
            default: "status@domain.com"
        - variable: MAIL_NAME
          label: "MAIL_NAME"
          description: "Container Variable MAILNAME"
          schema:
            type: string
            default: "Status"
        - variable: MAIL_ENCRYPTION
          label: "MAIL_ENCRYPTION"
          description: "Container Variable MAILENCRYPTION"
          schema:
            type: string
            default: "tls"
        - variable: REDIS_HOST
          label: "REDIS_HOST"
          description: "Container Variable REDISHOST"
          schema:
            type: string
            default: "192.168.1.100"
        - variable: REDIS_DATABASE
          label: "REDIS_DATABASE"
          description: "Container Variable REDISDATABASE"
          schema:
            type: string
            default: "0"
        - variable: REDIS_PORT
          label: "REDIS_PORT"
          description: "Container Variable REDISPORT"
          schema:
            type: string
            default: "6379"
        - variable: APP_DEBUG
          label: "APP_DEBUG"
          description: "Container Variable APPDEBUG"
          schema:
            type: string
            default: "false"

# Include{containerConfig}

  - variable: service
    group: "Networking and Services"
    label: "Configure Service(s)"
    schema:
      additional_attrs: true
      type: dict
      attrs:
        - variable: main
          label: "Main Service"
          description: "The Primary service on which the healthcheck runs, often the webUI"
          schema:
            additional_attrs: true
            type: dict
            attrs:
# Include{serviceSelector}
                    - variable: main
                      label: "Main Service Port Configuration"
                      schema:
                        additional_attrs: true
                        type: dict
                        attrs:
                          - variable: port
                            label: "Port"
                            description: "This port exposes the container port on the service"
                            schema:
                              type: int
                              default: 8000
                              required: true
                          - variable: advanced
                            label: "Show Advanced settings"
                            schema:
                              type: boolean
                              default: false
                              show_subquestions_if: true
                              subquestions:
                                - variable: protocol
                                  label: "Port Type"
                                  schema:
                                    type: string
                                    default: TCP
                                    enum:
                                      - value: HTTP
                                        description: "HTTP"
                                      - value: "HTTPS"
                                        description: "HTTPS"
                                      - value: TCP
                                        description: "TCP"
                                      - value: "UDP"
                                        description: "UDP"
                                - variable: nodePort
                                  label: "Node Port (Optional)"
                                  description: "This port gets exposed to the node. Only considered when service type is NodePort, Simple or LoadBalancer"
                                  schema:
                                    type: int
                                    min: 9000
                                    max: 65535
                                - variable: targetPort
                                  label: "Target Port"
                                  description: "The internal(!) port on the container the Application runs on"
                                  schema:
                                    type: int
                                    default: 8000



  - variable: serviceexpert
    group: "Networking and Services"
    label: "Show Expert Config"
    schema:
      type: boolean
      default: false
      show_subquestions_if: true
      subquestions:
        - variable: hostNetwork
          group: "Networking and Services"
          label: "Host-Networking (Complicated)"
          schema:
            type: boolean
            default: false

# Include{serviceExpert}

# Include{serviceList}

  - variable: persistence
    label: "Integrated Persistent Storage"
    description: "Integrated Persistent Storage"
    group: "Storage and Persistence"
    schema:
      additional_attrs: true
      type: dict
      attrs:

        - variable: appdata
          label: "appdata Storage"
          description: "Container Path varwwwCachet"
          schema:
            additional_attrs: true
            type: dict
            attrs:
              - variable: type
                label: "Type of Storage"
                description: "Sets the persistence type, Anything other than PVC could break rollback!"
                schema:
                  type: string
                  default: "simplePVC"
                  enum:
                    - value: "simplePVC"
                      description: "PVC (simple)"
                    - value: "simpleHP"
                      description: "HostPath (simple)"
                    - value: "emptyDir"
                      description: "emptyDir"
                    - value: "pvc"
                      description: "pvc"
                    - value: "hostPath"
                      description: "hostPath"
# Include{persistenceBasic}
              - variable: hostPath
                label: "hostPath"
                description: "Path inside the container the storage is mounted"
                schema:
                  show_if: [["type", "=", "hostPath"]]
                  type: hostpath
              - variable: medium
                label: "EmptyDir Medium"
                schema:
                  show_if: [["type", "=", "emptyDir"]]
                  type: string
                  default: ""
                  enum:
                    - value: ""
                      description: "Default"
                    - value: "Memory"
                      description: "Memory"
# Include{persistenceAdvanced}


# Include{persistenceList}


  - variable: ingress
    label: ""
    group: "Ingress"
    schema:
      additional_attrs: true
      type: dict
      attrs:
        - variable: main
          label: "Main Ingress"
          schema:
            additional_attrs: true
            type: dict
            attrs:
# Include{ingressDefault}

# Include{ingressTLS}

# Include{ingressTraefik}

# Include{ingressExpert}

# Include{ingressList}

# Include{security}

  - variable: advancedSecurity
    label: "Show Advanced Security Settings"
    group: "Security and Permissions"
    schema:
      type: boolean
      default: false
      show_subquestions_if: true
      subquestions:
        - variable: securityContext
          label: "Security Context"
          schema:
            additional_attrs: true
            type: dict
            attrs:
              - variable: privileged
                label: "Privileged mode"
                schema:
                  type: boolean
                  default: false
              - variable: readOnlyRootFilesystem
                label: "ReadOnly Root Filesystem"
                schema:
                  type: boolean
                  default: false
              - variable: allowPrivilegeEscalation
                label: "Allow Privilege Escalation"
                schema:
                  type: boolean
                  default: false
              - variable: runAsNonRoot
                label: "runAsNonRoot"
                schema:
                  type: boolean
                  default: false
# Include{securityContextAdvanced}

  - variable: podSecurityContext
    group: "Security and Permissions"
    label: "Pod Security Context"
    schema:
      additional_attrs: true
      type: dict
      attrs:
        - variable: runAsUser
          label: "runAsUser"
          description: "The UserID of the user running the application"
          schema:
            type: int
            default: 0
        - variable: runAsGroup
          label: "runAsGroup"
          description: The groupID this App of the user running the application"
          schema:
            type: int
            default: 0
        - variable: fsGroup
          label: "fsGroup"
          description: "The group that should own ALL storage."
          schema:
            type: int
            default: 568
# Include{podSecurityContextAdvanced}


# Include{resources}

# Include{advanced}

# Include{addons}
