apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "phpmyadmin.fullname" . }}
  labels:
    {{- include "phpmyadmin.labels" . | nindent 4 }}
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: {{ .Values.strategyType }}
  selector:
    matchLabels:
      {{- include "phpmyadmin.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "phpmyadmin.selectorLabels" . | nindent 8 }}
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
    spec:
    {{- if .Values.hostNetwork }}
      hostNetwork: {{ .Values.hostNetwork }}
      dnsPolicy: ClusterFirstWithHostNet
    {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
          - name: phpmyadmin
            protocol: TCP
            containerPort: 80
          {{- if .Values.hostNetwork }}
            hostPort: {{ .Values.phpmyadminService.port }}
          {{- end }}
          env:
          {{- if .Values.mysqlHost }}
          - name: PMA_HOST
            value: {{ .Values.mysqlHost }}
          {{ else }}
          - name: PMA_ARBITRARY
            value: "1"
          {{ end }}
          - name: TZ
            value: "{{ .Values.timezone }}"
          # phpmyadmin env vars
          - name: phpmyadmin_INTERNAL_ADDRESS
            value: http://{{ template "phpmyadmin.fullname" . }}:8989
          - name: phpmyadmin_IMAGE
            value: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          - name: KUBE_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace

# Extra ENV Values supplied by user
{{- range $key, $value := .Values.extraEnv }}
          - name: {{ $key }}
            value: {{ $value }}
{{- end }}

#          readinessProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            failureThreshold: 5
#            periodSeconds: 15
#          livenessProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            failureThreshold: 5
#            periodSeconds: 15
#          startupProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            initialDelaySeconds: 5
#            failureThreshold: 40
#            periodSeconds: 15
          volumeMounts:
          - name: config
            mountPath: /config
          - name: shared
            mountPath: /shared
          - name: shared-logs
            mountPath: "/config/Library/Application Support/phpmyadmin Media Server/Logs"
      volumes:
      - name: config
        {{- if .Values.emptyDirVolumes }}
        emptyDir: {}
        {{- else }}
        hostPath:
          path: {{ template "configuredHostPathConfig" . }}
        {{- end }}
      - name: shared
        emptyDir: {}
      - name: shared-logs
        emptyDir: {}
