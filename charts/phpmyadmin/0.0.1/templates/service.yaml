apiVersion: v1
kind: Service
metadata:
  {{- if .Values.hostNetwork }}
  name: {{ include "phpmyadmin.fullname" . }}-cluster-ip
  {{- else }}
  name: {{ include "phpmyadmin.fullname" . }}
  {{- end }}
  labels:
    {{- include "phpmyadmin.labels" . | nindent 4 }}
spec:
  {{- if .Values.hostNetwork }}
  type: ClusterIP
  {{- else if .Values.phpmyadminService.port }}
  type: NodePort
  {{- end }}
  ports:
    - name: phpmyadmin
      port: 80
      protocol: TCP
      targetPort: 80
      {{- if eq .Values.hostNetwork false }}
      nodePort: {{.Values.phpmyadminService.port}}
      {{- end }}
  selector:
    {{- include "phpmyadmin.selectorLabels" . | nindent 4 }}
