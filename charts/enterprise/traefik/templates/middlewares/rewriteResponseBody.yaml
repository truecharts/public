{{- range $index, $middlewareData := .Values.middlewares.rewriteResponseBody }}
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: {{ ternary (printf "%v-%v" $.Release.Name $middlewareData.name) $middlewareData.name $.Values.ingressClass.enabled }}
  namespace: {{ $.Release.Namespace }}
spec:
  plugin:
    plugin-rewritebody:
      lastModified: "true"
      rewrites:
        {{- range $index, $rewriteResponseBody := $middlewareData.headers }}
        - regex: {{ $rewriteResponseBody.regex | quote }}
          replacement: {{ $rewriteResponseBody.replacement | quote }}
        {{- end }}
{{- end -}}
