apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bazarr.fullname" . }}
  labels:
    {{- include "bazarr.labels" . | nindent 4 }}
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: {{ .Values.strategyType }}
  selector:
    matchLabels:
      {{- include "bazarr.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bazarr.selectorLabels" . | nindent 8 }}
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
    spec:
    {{- if .Values.hostNetwork }}
      hostNetwork: {{ .Values.hostNetwork }}
      dnsPolicy: ClusterFirstWithHostNet
    {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
          - name: bazarr
            protocol: TCP
            containerPort: 6767
          {{- if .Values.hostNetwork }}
            hostPort: {{ .Values.bazarrService.port }}
          {{- end }}
          env:
          - name: TZ
            value: "{{ .Values.timezone }}"
          # bazarr env vars
          - name: BAZARR_INTERNAL_ADDRESS
            value: http://{{ template "bazarr.fullname" . }}:6767
          - name: BAZARR_IMAGE
            value: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          - name: KUBE_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace

# Extra ENV Values supplied by user
{{- range $key, $value := .Values.extraEnv }}
          - name: {{ $key }}
            value: {{ $value }}
{{- end }}

#          readinessProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            failureThreshold: 5
#            periodSeconds: 15
#          livenessProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            failureThreshold: 5
#            periodSeconds: 15
#          startupProbe:
#            httpGet:
#              path: /identity
#              port: 32400
#            initialDelaySeconds: 5
#            failureThreshold: 40
#            periodSeconds: 15
          volumeMounts:
          - name: data
            mountPath: /data
          - name: config
            mountPath: /config
          - name: downloads
            mountPath: /downloads
          - name: shared
            mountPath: /shared
          - name: shared-logs
            mountPath: "/config/Library/Application Support/bazarr Media Server/Logs"
      volumes:
      - name: data
        {{- if .Values.emptyDirVolumes }}
        emptyDir: {}
        {{- else }}
        hostPath:
          path: {{ template "configuredHostPathData" . }}
        {{- end }}
      - name: config
        {{- if .Values.emptyDirVolumes }}
        emptyDir: {}
        {{- else }}
        hostPath:
          path: {{ template "configuredHostPathConfig" . }}
        {{- end }}
      - name: downloads
        {{- if .Values.emptyDirVolumes }}
        emptyDir: {}
        {{- else }}
        hostPath:
          path: {{ template "configuredHostPathdownloads" . }}
        {{- end }}
      - name: shared
        emptyDir: {}
      - name: shared-logs
        emptyDir: {}
