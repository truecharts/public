{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . -}}

{{/* Render secrets for radicale */}}
{{- $secrets := include "radicale.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "radicale-secrets" $secrets -}}
{{- end -}}

{{/* Render configmaps for all pods */}}
{{- $configmaps := include "radicale.configmaps" . | fromYaml -}}
{{- if $configmaps -}}
  {{- $_ := mustMergeOverwrite .Values.configmap $configmaps -}}
{{- end -}}

{{/* Render the templates */}}
{{- include "tc.v1.common.loader.apply" . -}}
