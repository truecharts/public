image:
  pullPolicy: IfNotPresent
  repository: tccr.io/truecharts/plex-playlist-sync
  tag: latest@sha256:68b580ff05514eab0735a83adb1b8873427435714c8b303c08cf335ce4e892dc

securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsNonRoot: false
    runAsGroup: 0
    runAsUser: 0

service:
  main:
    enabled: false
    ports:
      main:
        enabled: false

pps:
  plex:
    url: "http://localhost:32400"
    token: "plextoken123"
  spotify:
    user_id: ""
    client_id: ""
    client_secret: ""
  deezer:
    user_id: ""
    playlist_id: []
  other:
    write_missing_as_csv: false
    append_service_suffix: true
    add_playlist_poster: true
    add_playlist_description: true
    append_instead_of_sync: false
    seconds_to_wait: 84000

workload:
  main:
    podSpec:
      containers:
        main:
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
          env:
            PLEX_URL: "{{ .Values.ppl.plex.url }}"
            PLEX_TOKEN: "{{ .Values.ppl.plex.token }}"
            SPOTIFY_USER_ID: "{{ .Values.ppl.spotify.user_id }}"
            SPOTIFY_CLIENT_ID: "{{ .Values.ppl.spotify.client_id }}"
            SPOTIFY_CLIENT_SECRET: "{{ .Values.ppl.spotify.client_secret }}"
            DEEZER_USER_ID: "{{ .Values.ppl.deezer.user_id }}"
            DEEZER_PLAYLIST_ID: '{{ join " " .Values.deezer.playlist_id }}'
            WRITE_MISSING_AS_CSV: "{{ .Values.ppl.other.write_missing_as_csv }}"
            APPEND_SERVICE_SUFFIX: "{{ .Values.ppl.other.append_service_suffix }}"
            ADD_PLAYLIST_POSTER: "{{ .Values.ppl.other.add_playlist_poster }}"
            ADD_PLAYLIST_DESCRIPTION: "{{ .Values.ppl.other.add_playlist_description }}"
            APPEND_INSTEAD_OF_SYNC: "{{ .Values.ppl.other.append_instead_of_sync }}"
            SECONDS_TO_WAIT: "{{ .Values.ppl.other.seconds_to_wait }}"

persistence:
  data:
    enabled: true
    mountPath: /data

portal:
  open:
    enabled: false

