image:
  repository: quay.io/pussthecatorg/libremdb
  pullPolicy: IfNotPresent
  tag: latest@sha256:a2bf50efa78491d1cc14e4659a495dde3ae6257b3d102dfe1c3c9d72e62c1f49

env:
  # NEXT_PUBLIC_URL: "https://libremdb.iket.me"
  NEXT_TELEMETRY_DISABLED: 1
  AXIOS_USERAGENT: "axios/0.27.2"
  AXIOS_ACCEPT: "application/json, text/plain, */*"
  AXIOS_LANGUAGE: "en-US,en;q=0.5"
  USE_REDIS: true
  REDIS_URL:
    secretKeyRef:
      name: rediscreds
      key: url

service:
  main:
    ports:
      main:
        protocol: HTTP
        targetPort: 3000
        port: 10593

persistence:
  config:
    enabled: true
    mountPath: /opt/app

redis:
  enabled: true
  existingSecret: "rediscreds"

portal:
  enabled: true
