image:
  repository: leonismoe/ariang
  pullPolicy: IfNotPresent
  tag: 1.2.4

securityContext:
  readOnlyRootFilesystem: false

service:
  main:
    ports:
      main:
        protocol: HTTP
        targetPort: 8080
        port: 18080

persistence:
  config:
    enabled: true
    mountPath: "/build"