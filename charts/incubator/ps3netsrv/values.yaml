image:
  pullPolicy: IfNotPresent


service:
  main:
    ports:
      main:
        port: 38008
        protocol: HTTP
        targetPort: 38008

persistence:
  games:
    enabled: true
    mountPath: /games

portal:
  enabled: true
