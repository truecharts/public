{{- if .Values.mercuryParser.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "awesome-ttrss.mercuryParser.fullname" . }}
  labels:
    {{- include "awesome-ttrss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.mercuryParser.service.type }}
  ports:
    - port: {{ .Values.mercuryParser.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "awesome-ttrss.mercuryParser.selectorLabels" . | nindent 4 }}
{{- end }}