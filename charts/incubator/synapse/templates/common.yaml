{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Render secrets for synapse */}}
{{- $secrets := include "synapse.secret" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := mustMergeOverwrite .Values.secret $secrets -}}
{{- end -}}

{{/* Render configmap for synapse */}}
{{- $configmap := include "synapse.configmap" . | fromYaml -}}
{{- if $configmap -}}
  {{- $_ := set .Values.configmap "synapse" $configmap -}}
{{- end -}}

{{- $newMiddlewares :=  list "tc-opencors-headers" }}
{{- $_ := set .Values.ingress.main "fixedMiddlewares" $newMiddlewares -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
