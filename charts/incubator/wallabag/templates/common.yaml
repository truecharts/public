{{/* Make sure all variables are set properly */}}
{{- include "tc.common.loader.init" . }}

{{/* Render config */}}
{{- include "wallabag.config" . }}

{{/* Render secret */}}
{{- include "wallabag.secret" . }}

{{/* Render the templates */}}
{{ include "tc.common.loader.apply" . }}
