{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . -}}

{{/* Disable [IPC_LOCK] if requested */}}
{{- if not .Values.llama.mlock -}}
  {{- $_ := set .Values.workload.api.podSpec.containers.api.capabilities.add IPC_LOCK -}}
{{- end -}}

{{/* Render the templates */}}
{{- include "tc.v1.common.loader.apply" . -}}
