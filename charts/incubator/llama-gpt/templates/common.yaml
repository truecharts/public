{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . -}}

{{- if .Values.llama.mlock -}}
  {{- $capAdd := .Values.workload.api.podSpec.containers.api.capabilities.add -}}
  {{- $capAdd = (mustAppend $capAdd "IPC_LOCK") }}
  {{- $_ := set .Values.workload.api.podSpec.containers.api.capabilities "add" $capAdd -}}
{{- end -}}

{{/* Render the templates */}}
{{- include "tc.v1.common.loader.apply" . -}}
