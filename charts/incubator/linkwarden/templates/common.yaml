{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Render configmap for linkwarden */}}
{{- $configmapFile := include "linkwarden.config" . | fromYaml -}}
{{- if $configmapFile -}}
  {{- $_ := set .Values.configmap "linkwarden-config" $configmapFile -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
