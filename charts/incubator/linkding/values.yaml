image:
  repository: tccr.io/truecharts/linkding
  tag: v1.8.8@sha256:c5a15b48ef46e409d6d0fe0552280611bcbb148e5311f4ed0e9e12d2360e6578
  pullPolicy: IfNotPresent

securityContext:
  readOnlyRootFilesystem: false
  runAsNonRoot: false

podSecurityContext:
  runAsUser: 0
  runAsGroup: 0

secret:
  DJANGO_SUPERUSER_USERNAME: "superuser"
  DJANGO_SUPERUSER_EMAIL: "super@example.com"
  DJANGO_SUPERUSER_PASSWORD: "somesecret"

env:
  LD_SERVER_PORT: "{{ .Values.service.main.ports.main.port }}"
  # User Defined
  LD_REQUEST_TIMEOUT: 60
  LD_DISABLE_BACKGROUND_TASKS: false
  LD_DISABLE_URL_VALIDATION: false

service:
  main:
    ports:
      main:
        port: 10210

persistence:
  data:
    enabled: true
    mountPath: "/etc/linkding/data"

lifecycle:
  postStart:
    exec:
      command:
        - "/bin/bash"
        - "-c"
        - |
          if [ -f /etc/linkding/data/.superuserexists ]; then
            echo "Superuser has been created before. Skipping...";
          else
            echo "Creating superuser...";
            python manage.py createsuperuser --username=${DJANGO_SUPERUSER_USERNAME} --email=${DJANGO_SUPERUSER_EMAIL} --noinput;
            echo "This file is to ensure that init script won't try to create a superuser again and fail because it already exsits" > /etc/linkding/data/.superuserexists;
          fi;
