image:
  repository: bitnami/wordpress
  tag: 6.1.1@ssha256:be61f2e187daa98c603015962be0c42605857184c209b74a44954f2abbbbf849
  pullPolicy: IfNotPresent

securityContext:
  readOnlyRootFilesystem: false
  runAsNonRoot: false

podSecurityContext:
  runAsUser: 0
  runAsGroup: 0

env:
  WORDPRESS_DB_HOST:
    secretKeyRef:
      name: mariadbcreds
      key: plainhost
  WORDPRESS_DB_USER: "{{ .Values.mariadb.mariadbUsername }}"
  WORDPRESS_DB_PASSWORD:
    secretKeyRef:
      name: mariadbcreds
      key: mariadb-password
  WORDPRESS_DB_NAME: "{{ .Values.mariadb.mariadbDatabase }}"

service:
  main:
    ports:
      main:
        protocol: HTTP
        targetport: 80
        port: 10591

persistence:
  data:
    enabled: true
    mountPath: /var/www/html

mariadb:
  enabled: true
  mariadbUsername: wordpress
  mariadbDatabase: wordpress
  existingSecret: "mariadbcreds"

portal:
  enabled: true
