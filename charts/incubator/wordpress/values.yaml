image:
  repository: bitnami/wordpress
  pullPolicy: IfNotPresent
  tag: 6.1.1@sha256:ed41950556c7b28e0465fa173ad35d7c632fe1883e15761cbdb5dd340a5eeadf

securityContext:
  runAsNonRoot: false
  readOnlyRootFilesystem: false

podSecurityContext:
  runAsUser: 0
  runAsGroup: 0

secretEnv:
  wordpress_db_host:
    secretKeyRef:
      name: mariadbcreds
      key: plainhost
  wordpress_db_password:
    secretKeyRef:
      name: mariadbcreds
      key: mariadb-password

wordpress:
  user: "user"
  pass: "bitnami"
  email: "user@example.com"
  first_name: "True"
  last_name: "Charts"
  blog_name: "Truecharts Blog"

notifier:
  smtp:
    enabled: false
    # host: ""
    # port: ""
    # user: ""
    # pass: ""

envFrom:
  - configMapRef:
      name: '{{ include "tc.common.names.fullname" . }}-env-config'
  - secretRef:
      name: '{{ include "tc.common.names.fullname" . }}-env-secret'

service:
  main:
    ports:
      main:
        protocol: HTTP
        port: 10591

persistence:
  config:
    enabled: true
    mountPath: /bitnami/wordpress

mariadb:
  enabled: true
  mariadbUsername: wordpress
  mariadbDatabase: wordpress
  existingSecret: mariadbcreds

portal:
  enabled: true
