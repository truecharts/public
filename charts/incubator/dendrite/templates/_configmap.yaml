{{/* Define the secrets */}}
{{- define "dendrite.config" -}}
configmap:
  dendrite-config:
    enabled: true
    data:
      dendrite.yaml: |
    {{- .Values.dendriteConfig | toYaml | nindent 2 }}
{{- end -}}
