{{- include "common.setup" . }}

{{/* Append the hardcoded settings */}}
{{- define "webmap.harcodedValues" -}}
persistence:
  webmap-config:
    enabled: "true"
    mountPath: "/root/token.sha256"
    subPath: "hashed-token"
    type: "custom"
    volumeSpec:
      configMap:
        name: {{ printf "%v-install" (include "common.names.fullname" .) }}
{{- end -}}
{{- $_ := mergeOverwrite .Values (include "webmap.harcodedValues" . | fromYaml) -}}

{{- include "webmap.configmap" . }}

{{/* Render the templates */}}
{{ include "common.postSetup" . }}
