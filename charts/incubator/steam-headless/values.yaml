image:
  repository: tccr.io/truecharts/steam-headless
  pullPolicy: IfNotPresent
  tag: vlatest@sha256:e981d2ca1cc56e8e8588daf94c90d5565bfa7c10fb4691796a9c52aa575d2d61

securityContext:
  container:
    runAsNonRoot: false
    readOnlyRootFilesystem: false
    runAsUser: 0
    runAsGroup: 0

workload:
  main:
    podSpec:
      containers:
        main:
          env:
            PORT_NOVNC_WEB: "{{ .Values.service.main.ports.main.port }}"
            MODE: "primary"
            # MODE = secondary
            # DISPLAY: :0
            # HOST_DBUS: true
            WEB_UI_MODE: "vnc"
            ENABLE_VNC_AUDIO: "true"
            # NEKO_NAT1TO1: "" needs to be the ip of the host
            ENABLE_SUNSHINE: "false"
            # SUNSHINE_USER: ""
            # SUNSHINE_PASS: ""
            # USER_PASSWORD: ""
            ENABLE_EVDEV_INPUTS: "true"

service:
  main:
    ports:
      main:
        protocol: http
        port: 8083

persistence:
  home:
    enabled: true
    mountPath: /home/default
  games:
    enabled: true
    mountPath: /mnt/games
  input:
    enabled: true
    type: hostPath
    hostPath: /dev/input
    mountPath: /dev/input
    readOnly: true
  dbus:
    enabled: true
    type: hostPath
    hostPath: /run/dbus
    mountPath: /run/dbus
    readOnly: true

portal:
  open:
    enabled: true
