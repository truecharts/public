{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{/* Render secrets for hastyPasty */}}
{{- $secrets := include "hastyPasty.secret" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "hastyPasty-secrets" $secrets -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
