{{/* Make sure all variables are set properly */}}
{{- include "tc.common.loader.init" . }}

{{/* Render config */}}
{{- include "crowdsec.config" . }}

{{/* Render secret */}}
{{- include "crowdsec.secret" . }}

{{- $_ := set .Values.additionalContainers "init" (include "crowdsec.init" . | fromYaml) -}}
{{- $_ := set .Values.additionalContainers "lapi" (include "crowdsec.lapi" . | fromYaml) -}}
{{- $_ := set .Values.additionalContainers "dashboard" (include "crowdsec.dashboard" . | fromYaml) -}}

{{/* Render the templates */}}
{{ include "tc.common.loader.apply" . }}
