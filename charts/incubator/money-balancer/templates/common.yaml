{{- include "tc.v1.common.loader.init" . }}

{{/* Render secrets for moneybalancer */}}
{{- $secrets := include "moneybalancer.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "secrets" $secrets -}}
{{- end -}}

{{ include "tc.v1.common.loader.apply" . }}
