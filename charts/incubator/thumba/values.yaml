image:
  repository: bokker/thumba
  pullPolicy: IfNotPresent
  tag: 0.0.15@sha256:e3a98106c4fd294f20ded844afa9d026920b2f679fb6178dbf6ff04cc405d815
podSecurityContext:
  runAsGroup: 0
  runAsUser: 0
securityContext:
  readOnlyRootFilesystem: false
  runAsNonRoot: false
secretEnv:
  GOOGLE_SEARCH_KEY: key
  GOOGLE_SEARCH_CX: projectId
  THUM_KEY_ID: keyID
  THUM_KEY_SECRET: keySecret
env:
  NEXT_PUBLIC_PORT: "{{ .Values.service.main.ports.main.port}}"
  NEXT_PUBLIC_HOST: "http://localhost"
service:
  main:
    ports:
      main:
        protocol: HTTP
        port: 10598
persistence:
  config:
    enabled: true
    mountPath: /app/config
  video:
    enabled: true
    mountPath: /app/video
portal:
  enabled: true
