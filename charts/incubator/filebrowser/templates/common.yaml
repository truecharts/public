{{/* Make sure all variables are set properly */}}
{{- include "common.setup" . }}

{{/* Append the hardcoded settings */}}
{{- define "filebrowser.harcodedValues" -}}
persistence:
  focalboard-config:
    enabled: "true"
    mountPath: "/.filebrowser.json"
    subPath: "filebrowser-config"
    type: "custom"
    volumeSpec:
      configMap:
        name: {{ printf "%v-install" (include "common.names.fullname" .) }}
{{- end -}}
{{- $_ := mergeOverwrite .Values (include "filebrowser.harcodedValues" . | fromYaml) -}}

{{- include "filebrowser.configmap" . }}

{{/* Render the templates */}}
{{ include "common.postSetup" . }}
