image:
  repository: maptiler/tileserver-gl
  tag: v4.3.3
  pullPolicy: IfNotPresent

service:
  main:
    ports:
      main:
        protocol: HTTP
        port: 9080
        targetPort: 8080

persistence:
  data:
    enabled: true
    mountPath: /data

portal:
  enabled: true
