# With the bellow we test both container starts and that tpl is working inside extraArgs list
workload:
  main:
    podSpec:
      containers:
        main:
          args:
            - tunnel
            - --no-autoupdate
            - "{{ .Values.testTpl }}"

testTpl: "--hello-world"
